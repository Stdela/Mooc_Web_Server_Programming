Create functionality in the application that allows the application to create two database tables and a connection table between them. 
Database table names must be Studentand Course. The name of the connection board must be Enrollment.

The database table Studentmust have columns id, first_nameand last_name. The database table Coursemust have columns idand name. 
The join table Enrollmentmust have columns course_idand student_id, which are, as the name implies, reference keys to the tables Courseand Studentmaster keys.
