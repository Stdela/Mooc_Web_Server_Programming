1. Perform this task box hellothymeleaflocated on the HelloThymeleafControllercategory the following functionality:

The request to the root path /uses Thymeleaf to return src/main/resources/templates/the index.htmlfile in the folder to the user .
The request to the path /videouses Thymeleaf to return src/main/resources/templates/the video.htmlfile in the folder to the user .

2. Implement be the bottom of the folder src/main/resources/templates/on the file index.htmlpage, using h1, h2, ul, li, p, table, tr, and tdelements in a sensible manner.
