The task uses an application to send messages. 
Modify the application so that the /messagesresponse to a GET-type request to an address always contains the latest 5 messages (sorted by insertion time). 
