A story that needs a title and a protagonist has been implemented in the HTML file that comes with the task template. 
Implement package hellomodellocated in the HelloModelControllercategory of functionality that deals with root on future requests in the request and use the future parameters index.htmlof the file to complete the story.

The parameters that come with the request are called title and person. Both are stringy.

Add the values of the parameters included in the request to the Model object provided to Thymeleaf.
The title key must be "title"and the person's key must be "person". Returned to the site is in a folder src/main/resources/templates/in the index.html.
