There is an application in the task template that handles Itemobjects of type. Your task is to add the following additional functionality to the application:

When a user opens the root path of an application in a browser (i.e., makes a GET-type request to the root path of the application), 
a list of objects should be displayed in addition to his existing form.
Each object must have its name and type.
